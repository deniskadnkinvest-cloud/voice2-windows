# Voice2 — дорожная карта внедрения биллинга

Документ-инструкция для оператора: что делать руками, что делает ассистент, какие шаги и в каком порядке. Сегодня 2026-05-20.

Цель: за **2 недели** получить рабочую модель «2000 слов free → ЮKassa оплата → безлимит» на macOS. Windows-установщик с той же логикой — следом.

---

## 0. Что уже есть на сегодня

| Компонент | Статус | Где |
|---|---|---|
| Voice2.app для macOS | ✅ работает | `/Applications/Voice2.app` |
| macOS dev-сертификат (Voice2Dev) | ✅ есть скрипт | `setup-cert.sh` |
| Windows-сборка | 🟡 в работе | `build.ps1` + `VOICE2-WINDOWS.md` |
| EV-сертификат Windows | ❌ нет | — нужно покупать |
| ЮKassa магазин | ❌ нет | — нужно регистрировать |
| Бэкенд биллинга | ❌ нет | — нужно поднимать на Aeza |
| Юр.лицо (ИП) | ⚠️ уточнить | — нужно для ЮKassa |

---

## 1. Про EV-сертификат — что это и зачем 12 000 ₽/год

### Это сертификат подписи кода для Windows

Когда пользователь качает `Voice2-Setup.exe` и пытается запустить, Windows смотрит — **подписан ли файл известным сертификатом**:

| Подпись | Что видит пользователь |
|---|---|
| Без подписи | Большое красное окно SmartScreen: «Windows защитила ваш компьютер». Кнопка «Запустить всё равно» спрятана под «Подробнее». 80% людей закроют |
| Подпись OV (обычная) | Окно SmartScreen первые 1–2 недели, потом «репутация» нарастёт |
| Подпись EV (расширенная) | **Никакого SmartScreen с первого дня.** Доверие сразу |

### Это **только для Windows**

На macOS работает «ad-hoc подпись» (твой `Voice2Dev`) — это локальный сертификат, который ты сам генерируешь скриптом `setup-cert.sh`. Apple-у такого хватает, **macOS-юзеры ничего не заметят**.

Для строгой раздачи на Mac есть Apple Developer Program ($99/год + нотаризация) — но это не сейчас, для DMG распространения первой версии хватит ad-hoc подписи.

### Где брать EV для Windows в РФ (2026)

| Поставщик | Цена/год | Срок | Носитель |
|---|---|---|---|
| **ТЕНЗОР (КриптоПро)** | ~10 000 ₽ | 1 год | USB-токен (Рутокен) |
| **КриптоПро УЦ** | ~12 000 ₽ | 1 год | USB-токен |
| **СберКриптоПро** | ~15 000 ₽ | 1 год | Программный + токен |
| GlobalSign / Sectigo EV (через VPN) | $300–500 | 1 год | Cloud HSM |

**Рекомендую ТЕНЗОР** — дешевле всех, выпускают за 3–5 дней по ИП. Нужен будет физический Рутокен (≈1500 ₽ разово).

### Альтернатива на старте — не покупать EV

Первый месяц можно жить без EV:
- В установщике написать «Подпишите файл когда увидите SmartScreen → «Подробнее» → «Запустить всё равно»
- Когда наберётся 500–1000 установок — Microsoft сам поднимет репутацию OV-сертификата (3000 ₽/год вместо 12k)

Это нормальная стратегия — Wispr Flow и куча других стартапов так живут первые месяцы.

---

## 2. Архитектура MVP биллинга (минимум для теста за 1₽)

### Поток пользователя

```
[Voice2 macOS] первый запуск
     ↓
[Окно] «Введи email → получи magic link»
     ↓
[Backend] /api/auth/start → отправляет письмо
     ↓
[User] кликает ссылку → открывает voice2://login?token=xxx
     ↓
[Voice2] сохраняет токен локально → user_tier="free", words_used=0
     ↓
[User] диктует → после каждой диктовки
     ↓
[Voice2] POST /api/usage  { words: 42 }
     ↓
[Backend] увеличивает счётчик, возвращает { used: 1850, limit: 2000, tier: "free" }
     ↓
[Voice2] показывает в углу: "1850/2000 слов"
     ↓
... достигли 2000 ...
     ↓
[Voice2] Pill всплывает с текстом:
         «Лимит free исчерпан. Оплатить безлимит — [Открыть оплату]»
     ↓
[User] жмёт → открывается браузер на:
         https://api.voice2.ru/pay/start?email=...
     ↓
[Backend] создаёт платёж в ЮKassa (тест — 1 ₽, продакшен — 490 ₽)
     ↓
[Браузер] редирект на pay.yookassa.ru → юзер платит
     ↓
[ЮKassa webhook] POST /api/billing/webhook → user.tier = "pro"
     ↓
[Voice2] следующий /api/usage вернёт tier="pro" → блок снят
```

### Стек бэкенда (минимальный)

| Слой | Технология | Где живёт |
|---|---|---|
| Backend | Python 3.12 + FastAPI | Aeza VPS (claude-vps) |
| База | PostgreSQL 16 | там же, Docker |
| Очередь | не нужна на старте | — |
| Платежи | yookassa-python SDK | через REST |
| Email magic-link | SendPulse SMTP | бесплатный план до 12k писем/мес |
| Домен | `api.voice2.ru` | RU-Center, ~600 ₽/год |
| TLS | Caddy + Let's Encrypt | автомат |

### Схема БД (3 таблицы)

```sql
users (
  id UUID PRIMARY KEY,
  email TEXT UNIQUE NOT NULL,
  tier TEXT NOT NULL DEFAULT 'free',  -- free | pro | lifetime
  pro_until TIMESTAMP,                 -- NULL для free и lifetime
  created_at TIMESTAMP DEFAULT now()
);

usage (
  user_id UUID REFERENCES users(id),
  month TEXT NOT NULL,                 -- '2026-05'
  words INT NOT NULL DEFAULT 0,
  PRIMARY KEY (user_id, month)
);

payments (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  yookassa_payment_id TEXT UNIQUE,
  amount_rub INT NOT NULL,
  status TEXT NOT NULL,                -- pending | succeeded | canceled
  created_at TIMESTAMP DEFAULT now()
);
```

### Endpoints (5 штук — больше не надо)

| Метод | Путь | Что делает |
|---|---|---|
| POST | `/api/auth/start` | Email → отправить magic link |
| GET | `/api/auth/verify` | Token → JWT (хранится в приложении) |
| POST | `/api/usage` | `{words: 42}` → возвращает `{used, limit, tier}` |
| POST | `/api/pay/start` | Создать платёж в ЮKassa, вернуть URL |
| POST | `/api/billing/webhook` | ЮKassa дёргает → апгрейдит юзера |

**Объём кода:** ~400 строк Python. Делается за день.

---

## 3. Тестовый режим за 1 ₽ — как настроить

ЮKassa поддерживает **тестовый магазин** — это твой первый шаг. Не платишь налогов, не сдаёшь чеки, можно гонять что угодно.

### Шаги
1. Регистрируешься на `yookassa.ru` (любой email)
2. Создаёшь **тестовый магазин** — не требует ИП, никаких документов
3. Получаешь `shopId` (число) и `secretKey` (`test_xxx`)
4. Платежи проходят **виртуально** — карты тестовые: `5555 5555 5555 4477` (успех), `5555 5555 5555 4444` (отказ)
5. Webhook'и реальные — приходят на твой бэкенд

### Когда переходим на боевой
1. Открываешь ИП (УСН 6% — 1 день через Тинькофф)
2. В ЮKassa личном кабинете подаёшь анкету на боевой магазин
3. Загружаешь скан паспорта + ОГРНИП
4. Через 1–3 дня — боевые ключи `live_xxx`
5. В `.env` заменяешь `YOOKASSA_SECRET=test_xxx` на `live_xxx`

### Цена 1 ₽
В тестовом и в боевом магазине — задаётся параметром платежа:
```python
Payment.create({
    "amount": {"value": "1.00", "currency": "RUB"},
    ...
})
```
Можно поставить хоть 1 ₽, хоть 490 ₽. На время теста — 1 ₽.

---

## 4. Windows-установщик с внутренней оплатой

Та же логика, что на Mac, только клиент написан на .NET 8 / WPF.

### Что встроено в `.exe` установщик
- `Voice2.exe` — приложение
- `whisper.exe` + `ggml-base.bin` — локальное распознавание
- Тот же magic-link login через `voice2://` URL-схему (Inno Setup её регистрирует)
- Тот же лимит 2000 слов / месяц
- Та же кнопка «Оплатить» → открывается браузер на `api.voice2.ru/pay/start`

### Установщик
Через **Inno Setup 6** — бесплатно, скачивается, пишется `Voice2.iss` (~80 строк). На выходе один файл `Voice2-Setup-X.Y.Z.exe`.

### Подпись
- **Без EV** — пользователь видит SmartScreen, проходит «Подробнее → Запустить» (плохой UX, но работает)
- **С EV** — устанавливается без вопросов

### Авто-обновления
- При запуске Voice2 → GET `api.voice2.ru/version`
- Если новее — качаем `.exe` в `%LocalAppData%\Voice2\update\`
- При следующем запуске — обновляемся в фоне через **Squirrel.Windows**

---

## 5. Дорожная карта — что когда делаем

### 🟢 Неделя 1 (20–26 мая) — тестовый биллинг на Mac

| День | Кто | Что |
|---|---|---|
| Вт 20.05 | Ты | Зарегистрироваться на `yookassa.ru` → тестовый магазин → выдать ассистенту `shopId` + `test_secret_key` |
| Ср 21.05 | Ты | Купить домен `voice2.ru` на RU-Center (~600 ₽) |
| Ср 21.05 | Ассистент | Поднять FastAPI скелет на Aeza VPS, настроить Caddy под `api.voice2.ru` |
| Чт 22.05 | Ассистент | Подключить Postgres, накатить миграции (3 таблицы), запилить 5 endpoints |
| Пт 23.05 | Ассистент | Интегрировать magic-link через SendPulse |
| Пт 23.05 | Ты | Создать аккаунт SendPulse (бесплатный), выдать SMTP-креды |
| Сб 24.05 | Ассистент | Добавить в Voice2.app (macOS) экран логина + счётчик слов + кнопку «Оплатить» |
| Вс 25.05 | Ассистент | Прогон end-to-end: диктуем → счётчик растёт → достигаем лимита (для теста = 50 слов) → платим 1 ₽ тестовой картой → tier=pro → блок снят |
| Пн 26.05 | Ты | Проверяешь сам, фиксим что не так |

**Итог недели 1:** Mac-версия с биллингом, тестовый магазин ЮKassa, лимит 50 слов (для теста), оплата 1 ₽.

### 🟢 Неделя 2 (27 мая – 2 июня) — Windows MVP + ИП

| День | Кто | Что |
|---|---|---|
| Вт 27.05 | Ты | Открыть ИП через Тинькофф (онлайн, паспорт + ИНН, занимает 30 минут) |
| Ср 28.05 | Ассистент | Доработать `build.ps1` — собрать `Voice2.exe` + установщик `Voice2-Setup.exe` через Inno Setup |
| Чт 29.05 | Ассистент | Добавить в Windows-версию: логин, счётчик, оплата (тот же бэкенд) |
| Пт 30.05 | Ассистент | Прогон end-to-end на Windows (можно через Parallels или виртуалку) |
| Сб 31.05 | Ты | Тест дать другу с Win11 → пусть установит → проверим SmartScreen |
| Вс 01.06 | Оба | Фиксы по фидбеку |
| Пн 02.06 | Ты | Загрузить документы в ЮKassa для боевого магазина |

**Итог недели 2:** Windows-версия с биллингом, заявка на боевой магазин подана.

### 🟡 Неделя 3 (3–9 июня) — переход в прод

| День | Кто | Что |
|---|---|---|
| Вт 03.06 | Ассистент | Поставить реальный лимит 2000 слов в month |
| Ср 04.06 | Ассистент | Цена 490 ₽ месяц + 3990 ₽ год — два варианта на экране оплаты |
| Чт 05.06 | Ассистент | Email после оплаты с чеком |
| Пт 06.06 | Ты | Получить боевые ключи ЮKassa → выдать ассистенту |
| Сб 07.06 | Ассистент | Переключить .env на live-ключи, прогон тест-продажи на себе |
| Вс 08.06 | Оба | Релиз 2.1.0 — публикуем DMG + EXE на сайт voice2.ru |
| Пн 09.06 | Ты | Заказ EV-сертификата ТЕНЗОР (если хочешь без SmartScreen) |

### 🟡 Неделя 4 (10–16 июня) — EV + дистрибуция

| День | Что |
|---|---|
| Чт 12.06 | Получение Рутокена с EV-сертификатом курьером (3–5 рабочих дней после оплаты) |
| Пт 13.06 | Установка драйверов Рутокен на твой Mac/Win, импорт сертификата |
| Пт 13.06 | Пересборка `Voice2-Setup.exe` с EV-подписью |
| Сб 14.06 | Тестовая раздача — нет SmartScreen |
| Вс 15.06 | Маркетинг-старт: пост в ТГ-канале, anonse |

---

## 6. Что нужно от тебя СЕЙЧАС (action items)

Чтобы ассистент стартовал, нужны от тебя 3 действия в ближайшие 2–3 дня:

### 1. ЮKassa тестовый магазин (15 мин)
1. Идёшь на https://yookassa.ru/joinups
2. Регистрируешься через email
3. В личном кабинете создаёшь **тестовый магазин** (без документов)
4. Берёшь `shopId` и `secret_key` (начинается на `test_`)
5. Кидаешь мне в чат — я сохраню в `~/.claude/.env` под ключами `YOOKASSA_SHOP_ID_TEST` и `YOOKASSA_SECRET_TEST`

### 2. Домен (15 мин)
1. https://nic.ru/catalog/order/multi/ → ищешь `voice2.ru`
2. Если занят — берёшь `voice2.app` или `voice2.io`
3. Платишь ~600 ₽, NS оставляешь дефолтные
4. Скажешь мне домен — настрою DNS на Aeza VPS

### 3. SendPulse (5 мин)
1. https://sendpulse.com/ru/signin → регистрация
2. В разделе SMTP — генерируешь логин/пароль
3. Бесплатный план = 12k писем/мес, нам хватит на 10 000 юзеров
4. Креды кинуть мне → сохраню в `.env`

### Опционально на этой неделе
- ИП открыть — можно подождать до недели 2, бесплатно через Тинькофф онлайн
- EV-сертификат — заказывать после релиза, не блокирует MVP

---

## 7. Сколько это всё стоит

### Разовые расходы
| Что | Цена |
|---|---|
| Домен voice2.ru на год | 600 ₽ |
| ИП открытие | 0 ₽ (бесплатно через банк) |
| Госпошлина за ИП | 800 ₽ |
| Рутокен ЭЦП 3.0 | 1 500 ₽ |
| **Итого старт** | **2 900 ₽** |

### Ежемесячные расходы
| Что | Цена/мес |
|---|---|
| Aeza VPS (уже есть) | 199 ₽ |
| SendPulse SMTP | 0 ₽ (бесплатный план) |
| ЮKassa комиссия | 3.5% от оборота |
| **Итого** | **199 ₽ + 3.5%** |

### Ежегодные
| Что | Цена/год |
|---|---|
| EV-сертификат ТЕНЗОР | 10 000 ₽ |
| Домен | 600 ₽ |
| **Итого** | **10 600 ₽** |

### Break-even
- Один Pro-юзер в год даёт **3 990 ₽** (или 5 880 ₽ если месячные)
- Минус ~3.5% комиссии = ~3 850 ₽
- **3 платных юзера/год покрывают всю инфраструктуру**
- 4-й юзер уже даёт чистый профит

---

## 8. Решения, которые нужно подтвердить от тебя

| Вопрос | Варианты | Моё предложение |
|---|---|---|
| Стартуем неделю 1 как написано? | Да / нет / переделать сроки | Да |
| Хоткей Windows | Right Ctrl (рекомендую) / Caps Lock / F-key | Right Ctrl |
| Лимит free | 2000 слов/мес (как Aqua) / 1000 / 5000 | 2000 |
| Цена Pro месяц | 490 ₽ / 390 ₽ / 590 ₽ | 490 ₽ |
| Цена Pro год | 3990 ₽ / 3490 ₽ / 4990 ₽ | 3990 ₽ |
| Lifetime оффер | да, 9990 ₽ на первые 100 / нет | Да, маркетинг-хук |
| Бэкенд деплоить на Aeza или новый VPS | Aeza (уже есть) / новый | Aeza |
| EV сразу или после 100 установок | Сразу / после | После 100 установок |

---

## 9. Что НЕ делаем на старте (явный scope-cut)

- ❌ Авто-возобновление подписки (рекуррент) — пока ручная оплата раз в месяц
- ❌ Кабинет пользователя на сайте — настройки внутри приложения
- ❌ Команда / организации — только индивидуальные подписки
- ❌ Linux версия
- ❌ Mobile (iOS/Android)
- ❌ Облачное распознавание — мы локальные, это наше преимущество
- ❌ Промокоды — добавим если будут партнёры
- ❌ Реферальная программа — после 500 пользователей

---

*Карта живая. После каждой недели обновляем чек-листы галочками и фиксируем что съехало.*
